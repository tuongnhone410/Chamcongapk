
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.timesnap.pro',
  appName: 'TimeSnap Pro',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
